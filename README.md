# Notes:

My first node program from scratch.

```
Options:
      --help     Show help                                        [boolean]
      --version  Show version number                              [boolean]
  -n, --num      Inputs the number to multiply          [number] [required]
  -l, --list     Outputs the table in console  [boolean] [default: "false"]
  -m, --max      Limits the table lenght to an specific number     [number]
```
